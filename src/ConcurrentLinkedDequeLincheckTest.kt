import java.util.concurrent.*

class ConcurrentLinkedDequeLincheckTest {
    private val d = ConcurrentLinkedDeque<Int>()

    // TODO:
    //  1. Add `addFirst(..)`, `addLast(..)`,
    //    `peekFirst()`, `peekLast()`,
    //    `pollFirst()`, and `pollLast()` operations.
    //  2. Add a `test()` function annotated with `@Test`
    //     that runs the Lincheck test in the model-checking mode.
    //  3. Run the test!
}
